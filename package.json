{
  "name": "maandamano-tracker",
  "version": "1.0.0",
  "description": "A protest activity tracking system",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@octokit/rest": "^19.0.7"
  },
  "engines": {
    "node": ">=16.x"
  }
}